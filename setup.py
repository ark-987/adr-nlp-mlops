from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='ADR classification using NLP of noisy dataset with transformer driven by an engine within MLOPs workflow',
    author='ark',
    license='MIT',
)
